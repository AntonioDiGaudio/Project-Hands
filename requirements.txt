# Dipendenze effettivamente usate da AirMouse.
#
# Il file precedente elencava 38 pacchetti, fra cui l'intero albero transitivo
# di mediapipe (jax, jaxlib, matplotlib, scipy, sentencepiece...) piu' roba mai
# importata dal progetto (pyarmor, altgraph, pyinstaller-hooks-contrib) e
# python3-xlib, che su Windows non e' nemmeno installabile.

mediapipe==0.10.21
opencv-python==4.11.0.86
numpy>=1.26,<2          # mediapipe 0.10.x non e' compatibile con numpy 2.x
PyAutoGUI==0.9.54

# Solo su Linux: pyautogui usa Xlib per muovere il cursore.
python3-xlib==0.15; sys_platform == "linux"
